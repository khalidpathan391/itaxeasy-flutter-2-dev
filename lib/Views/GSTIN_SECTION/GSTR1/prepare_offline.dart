import 'package:flutter/material.dart';

class PrepareOffline extends StatefulWidget {
  const PrepareOffline({Key key}) : super(key: key);

  @override
  _PrepareOfflineState createState() => _PrepareOfflineState();
}

class _PrepareOfflineState extends State<PrepareOffline> {
  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(
      body: SingleChildScrollView(
        child: Column(
          children: const [],
        ),
      ),
    ) );
  }
}
